# Storage Model

The extension stores data through the platform storage adapter.

Prefixes:

- `workflow:`
- `capsule:`
- `history:`
- `pref:`

Export/import uses a versioned JSON bundle with workflows, capsules, and optional preferences.
